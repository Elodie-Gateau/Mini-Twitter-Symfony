{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class = "w-100 flex justify-center">
    <button class="bg-transparent hover:bg-green-500 text-green-700 font-semibold hover:text-white py-2 px-4 border border-green-500 hover:border-transparent rounded cursor-pointer mt-2">{{ button_label|default('Sauvegarder') }}</button>
    </div>
{{ form_end(form) }}
