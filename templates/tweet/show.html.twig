{% extends 'base.html.twig' %}

{% block title %}Message{% endblock %}

{% block body %}
    <h1>Message</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Message</th>
                <td>{{ tweet.content }}</td>
            </tr>
            <tr>
                <th>Date d'ajout</th>
                <td>{{ tweet.creationTime ? tweet.creationTime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tweet_index') }}">Retour à la liste</a>

    <a href="{{ path('app_tweet_edit', {'id': tweet.id}) }}">Modifier</a>

    {{ include('tweet/_delete_form.html.twig') }}
{% endblock %}
