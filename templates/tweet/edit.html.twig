{% extends 'base.html.twig' %}

{% block title %}Mdofier le message{% endblock %}

{% block body %}
    <h1>Mdofier le message</h1>

    {{ include('tweet/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_tweet_index') }}">Retour à la liste</a>

    {{ include('tweet/_delete_form.html.twig') }}
{% endblock %}
